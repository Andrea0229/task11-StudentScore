// 'use strict';

const optionList = require("../main/optionList");
// const main = require('../main/main');

/*
describe("命令行菜单栏列表", function () {
    it("命令行-选项列表1：", function () {
        let options = optionList();
        let result = options[0];
        let expect_str = "1. 添加学生";

        expect(expect_str).toEqual(result);
    });

    it("命令行所有选项列表-getOptions方法：", function () {
        // spyOn(console.log());
        let options = optionList().join('\n');
        let result = `1. 添加学生\n2. 生成成绩单\n3. 退出`;

        expect(options).toEqual(result);
    })
});

describe("命令行选项", function () {
    it("选择1：", function () {
        // let result = selectOption(1);
        // spyOn(console, 'log');
        // selectOption(1);
        // let expect_str = `请输入学生信息（格式：姓名, 学号, 班级, 学科: 成绩, ...），按回车提交：`;
        // expect(expect_str).toEqual(result);

        // expect(console.log).toHavaBeenCalledWith(expect_str);

    });

    /!*it("选择2：",function () {
        let result = selectOption(2);
        // let espect_str = `请输入要打印的学生的学号（格式： 学号, 学号,...），按回车提交：`;

        expect(espect_str).toEqual(result);
    });*!/
});*/
